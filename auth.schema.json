{
  "$schema": "http://json-schema.org/draft-07/schema",
  "title": "Mixcloud Uploader CLI authentication",
  "properties": {
    "access-token": {
      "type": "string",
      "description": "The Mixcloud API OAuth2 access token to use. Will take precedence over client-id and client-secret."
    },
    "client-id": {
      "type": "string",
      "description": "The Mixcloud API OAuth2 client id to use."
    },
    "client-secret": {
      "type": "string",
      "description": "The Mixcloud API OAuth2 client secret to use."
    }
  }
}
